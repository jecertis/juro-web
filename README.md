# Juro Web

This repository is reserved as the canonical web frontend repository (`juro-web`) for `jurocompliant.com`.

- **`jurocompliant.html`** — DPDP URL scanner UI (static page; expects `POST /api/scan` on the same origin). Local dev: run the Flask app in `juro/juro` and open `http://localhost:5001/` — it serves this file from `juro-web`.

Naming standard (repos/domains) source of truth: `/Users/arshdeep/git/juro-platform/contracts/nomenclature.md`.

Functionality contribution guide: `HOW_TO_CONTRIBUTE_FUNCTIONALITIES.md`.
